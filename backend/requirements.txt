# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
websockets==14.1
python-dotenv==1.0.1

# Stellar + HTTP + SEP-1 Discovery
stellar-sdk==12.1.0
httpx==0.28.1
tomli==2.2.1; python_version < "3.11"  # tomllib is built in from 3.11

# AI / RAG
google-generativeai==0.8.5
numpy==2.1.3

# Persistence (users + NegotiationSession + tx_hash)
sqlalchemy==2.0.36

# Passkeys / WebAuthn (FaceID, TouchID, Windows Hello)
webauthn==2.5.0
